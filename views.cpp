#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "../controllers/controllers.h"


int main {
   scanf ("%d %d", &t.totalPatient, &t.totalCure);
   int n=t.totalpatient;

   for (int i=0; i<n; i++){
       scanf ("%d %[^\n] %d - %[^\n]", &p.day, p.month, &p.year, p.name);
   }
   
   int out=t.totalPatient - t.totalCure
   printf ("Need %d more cure\n",out); 
   for (int j=0; j<out; j++){

   }

   return 0;
}
